# Basic Git Setup on your system

Run this command once in your terminal window:

```sh
git config --global user.name "[firstname lastname]"
git config --global user.email "[email]"
git config --global color.ui auto
```
